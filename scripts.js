function drawTree (treeHeight) {
    for ( var i = 0 ; i < 5 ; i++) {
        var star = '';
        for ( var j = 0 ; j < i ; j = i) {
        var star += '*';
       };
       console.log(star);
    };
}